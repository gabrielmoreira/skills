# Add invoice retrieval and receipt upload endpoints

Adds GET /invoices/:id and POST /invoices/:id/receipt.
Authentication middleware protects routes.

- [x] unit tests passing
- [x] auth middleware attached
