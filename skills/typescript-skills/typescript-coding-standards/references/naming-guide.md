# Naming Guide

Good names help the reader answer:
- what kind of thing is this?
- why does it exist?
- where should it change?

Prefer names that expose responsibility over names that only expose pattern.
