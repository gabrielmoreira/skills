import type { EvalScenario } from "./evals.types.ts";

const scenarios = [
  {
    id: "internal-route-classify-caller-fault-before-retry",
    bundle: "typescript-error-handling",
    rule: "error-classification",
    tier: "P0",
    mode: "router",
    difficulty: "hard",
    prompt:
      "A payment client retries every failure three times, including a stable 400 'card declined'. Which canonical decision should own the fix before retry mechanics are changed?",
    expectedPrimary: "skill://typescript-skills/typescript-error-handling/rules/error-classification.md",
    expectedSecondary: ["skill://typescript-skills/typescript-async/rules/retry-and-backoff.md"],
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-async/rules/retry-and-backoff.md"],
    },
    must: ["Routes primary ownership to failure classification before retry policy"],
    mustNot: ["Lets retry timing own caller-fault semantics"],
    tags: ["activation", "internal-route", "wrong-owner", "retry"],
  },
  {
    id: "internal-route-worker-process-lifecycle",
    bundle: "typescript-async",
    rule: "process-lifecycle",
    tier: "P0",
    mode: "router",
    difficulty: "mixed",
    prompt:
      "A long-running worker receives SIGTERM while jobs are active. It must stop intake, drain within the grace period, abort leftovers, and close resources. What exact rule owns this?",
    expectedPrimary: "skill://typescript-skills/typescript-async/rules/process-lifecycle.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-composition/rules/composition-root.md"],
    },
    must: ["Routes to process lifecycle ownership"],
    mustNot: ["Lets dependency assembly own shutdown semantics"],
    tags: ["activation", "internal-route", "lifecycle"],
  },
  {
    id: "internal-route-vendor-response-mapping",
    bundle: "typescript-boundaries",
    rule: "provider-containment",
    tier: "P0",
    mode: "router",
    difficulty: "mixed",
    prompt:
      "our order service takes the raw Stripe client object directly and reads nested fields deep inside the checkout loop. where should we convert third-party vendor types into our own internal models?",
    expectedPrimary: "skill://typescript-skills/typescript-boundaries/rules/provider-containment.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-boundaries/rules/raw-input-to-internal-model.md"],
    },
    must: ["Routes generated SDK type containment to the provider boundary rule"],
    mustNot: ["Treats a typed provider response as generic raw input"],
    tags: ["activation", "internal-route", "boundary"],
  },
  {
    id: "internal-route-provider-selected-at-startup",
    bundle: "typescript-composition",
    rule: "composition-root",
    tier: "P0",
    mode: "router",
    difficulty: "hard",
    prompt:
      "The application chooses one mail provider during startup, but feature code still switches providers per request. Where should provider selection and construction live?",
    expectedPrimary: "skill://typescript-skills/typescript-composition/rules/composition-root.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-boundaries/rules/provider-containment.md"],
    },
    must: ["Routes startup selection and construction to the composition owner"],
    mustNot: ["Lets provider type containment own runtime selection"],
    tags: ["activation", "internal-route", "composition"],
  },
  {
    id: "internal-route-secret-source-pointer",
    bundle: "typescript-security",
    rule: "secrets-lifecycle",
    tier: "P0",
    mode: "router",
    difficulty: "hard",
    prompt:
      "A config field contains a vault key reference and falls back to a plaintext token in development. Which rule owns loading timing, source precedence, and removal of that fallback?",
    expectedPrimary: "skill://typescript-skills/typescript-security/rules/secrets-lifecycle.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-configs/rules/defaults-and-ownership.md"],
    },
    must: ["Routes secret source and fallback semantics to security"],
    mustNot: ["Lets general configuration defaults own credential fallback"],
    tags: ["activation", "internal-route", "security"],
  },
  {
    id: "internal-route-non-secret-config-default",
    bundle: "typescript-configs",
    rule: "defaults-and-ownership",
    tier: "P1",
    mode: "router",
    difficulty: "mixed",
    prompt:
      "A feature's batch size and display mode need production-safe defaults, one owner, and explicit environment overrides. No credential or sensitive value is involved. Which rule owns this?",
    expectedPrimary: "skill://typescript-skills/typescript-configs/rules/defaults-and-ownership.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-security/rules/secrets-lifecycle.md"],
    },
    must: ["Routes ordinary behavior defaults to configuration ownership"],
    mustNot: ["Escalates non-sensitive defaults into secret lifecycle guidance"],
    tags: ["activation", "internal-route", "config"],
  },
  {
    id: "internal-route-config-in-test",
    bundle: "typescript-testing",
    rule: "config-in-tests",
    tier: "P0",
    mode: "router",
    difficulty: "mixed",
    prompt:
      "Tests mutate `process.env` globally and pass in isolation but fail in the suite. The production parser is already correct. What exact rule owns the test fix?",
    expectedPrimary: "skill://typescript-skills/typescript-testing/rules/config-in-tests.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-configs/rules/contextual-config.md"],
    },
    must: ["Routes test environment isolation to testing guidance"],
    mustNot: ["Redesigns the production configuration parser"],
    tags: ["activation", "internal-route", "testing"],
  },
  {
    id: "internal-route-trace-correlation",
    bundle: "typescript-observability",
    rule: "tracing-boundary",
    tier: "P1",
    mode: "router",
    difficulty: "hard",
    prompt:
      "Requests cross handlers, a queue, and a worker, but support cannot correlate the spans around an external provider call. Error semantics are already stable. Which rule owns the instrumentation boundary?",
    expectedPrimary: "skill://typescript-skills/typescript-observability/rules/tracing-boundary.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: ["skill://typescript-skills/typescript-error-handling/rules/error-shape-and-metadata.md"],
    },
    must: ["Routes trace propagation and instrumentation placement to observability"],
    mustNot: ["Changes the error model to solve missing trace correlation"],
    tags: ["activation", "internal-route", "observability"],
  },
  {
    id: "internal-route-top-down-function-order",
    bundle: "typescript-coding-standards",
    rule: "vertical-discipline",
    tier: "P1",
    mode: "router",
    difficulty: "mixed",
    prompt:
      "An exported orchestration function sits below its private helpers, so readers repeatedly scroll backward to follow the module. Names and behavior are already clear; only source order is in question, and no dependency wiring changes are needed. Which rule owns the layout decision?",
    expectedPrimary: "skill://typescript-skills/typescript-coding-standards/rules/vertical-discipline.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: [
        "skill://typescript-skills/typescript-coding-standards/rules/naming-and-semantic-center.md",
        "skill://typescript-skills/typescript-composition/rules/composition-root.md",
      ],
    },
    must: ["Routes module reading order to vertical layout guidance"],
    mustNot: ["Treats source order as naming or dependency construction"],
    tags: ["activation", "internal-route", "layout"],
  },
  {
    id: "internal-route-public-error-projection",
    bundle: "typescript-error-handling",
    rule: "error-boundary-contract",
    tier: "P0",
    mode: "router",
    difficulty: "hard",
    prompt:
      "An HTTP handler sends raw SDK exception messages and vendor fields to clients. Internal logs may retain diagnostics, but the public response needs a stable sanitized contract. Which exact rule owns the projection?",
    expectedPrimary: "skill://typescript-skills/typescript-error-handling/rules/error-boundary-contract.md",
    activation: {
      layer: "internal-route",
      target: "typescript-skills",
      shouldActivate: true,
      forbiddenRoutes: [
        "skill://typescript-skills/typescript-boundaries/rules/raw-input-to-internal-model.md",
        "skill://typescript-skills/typescript-observability/rules/redaction.md",
      ],
    },
    must: ["Routes public error projection to the error boundary contract"],
    mustNot: ["Lets inbound mapping or log redaction own the outbound API contract"],
    tags: ["activation", "internal-route", "public-contract"],
  },
] satisfies EvalScenario[];

export default scenarios;
