# Provider Language vs Local Language

Use provider terms at the edge when you must talk to the provider.
Use local terms inside the app when they help readers understand the app faster.

Good sign:
- provider types stay near adapters
- local types describe local meaning

Bad sign:
- business logic sounds like copied API docs
